#!/usr/bin/env ruby

require_relative '../lib/opener/social_media_classifier'

raise ArgumentError("Usage: social-media-classifier type input.arff output.arff") if ARGV.count != 3
kernel = Opener::SocialMediaClassifier.new(ARGV[0], ARGV[1], ARGV[2])

kernel.classify
